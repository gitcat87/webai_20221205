#include <stdio.h>

int main(){

    int num = 0;
    while(num <5)
    {
            printf("Hello World %d\n",num);
            num++;
    }
    return 0;
}

// ctrl + y 앞으로 가기
// alt + shift + f 자동정렬